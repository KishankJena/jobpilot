"""
Alembic migration scripts for JobPath live in this package.

Generate a new migration after model changes with a command like:

    alembic -c alembic.ini revision --autogenerate -m "describe change"

Then apply migrations with:

    alembic -c alembic.ini upgrade head
"""

